BeFit – aplikacja ASP.NET Core MVC (.NET 8) + EF Core + Identity

1. Ustaw connection string w appsettings.json (DefaultConnection).
2. W konsoli:
   - dotnet ef database update
   - dotnet run
3. Logowanie jako administrator:
   - login: admin@befit.local
   - hasło: Admin123!
